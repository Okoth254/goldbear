---
description: Run Riverpod and Freezed code generation
---
Run `build_runner` to generate the `.g.dart` and `.freezed.dart` files whenever Riverpod providers or Freezed models change.

// turbo-all
1. Run `dart run build_runner build -d` in the root directory (`c:\Desktop\goldbear`).
