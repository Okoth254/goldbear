---
description: Build Android APK for the Flutter app
---
Build a release APK for Android deployment/testing.

// turbo-all
1. Run `flutter clean` in the root directory.
2. Run `flutter pub get` in the root directory.
3. Run `flutter build apk --release` in the root directory.
