---
description: Lint and Format Dart project
---
Format the Dart codebase and run the dart analyzer to catch any errors.

// turbo-all
1. Run `dart format .` in the root directory.
2. Run `dart analyze` in the root directory.
